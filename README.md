# Beholders
In order to set the number of threads, use the rayon random variables. The default is:
## Run prover
```
cargo run --bin prover
```
## Run verifier
```
cargo run --bin verifier
```
## Run test
```
cargo nextest run
```
## Run benchmarks
See [benches/README.md](benches/README.md)